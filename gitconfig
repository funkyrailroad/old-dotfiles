[core]
	editor = vim
[user]
	email = jatwell@physik.fu-berlin.de
	name = Jake Atwell
[alias]
    a   = add
    aa  = add .
    ap  = add --patch
    b   = branch
    cm  = commit
    cma = commit --amend
	co  = checkout
    ev  = !vim ~/.gitconfig
    ek  = !vim ~/.vim/keepers/git.rst
    d   = diff
    ;l   = log --stat
    l = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
    m   = merge 
    s   = status
    sh  = show
[push]
	default = simple
