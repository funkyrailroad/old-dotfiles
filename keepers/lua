how to write text to stdout in lua
io.write(), print()

exit a lua script
os.exit()

pairs(t) 
gives a general iterator over all keys and values in a table (no particlar
order)

ipairs(t) 
gives an iterator that goes over the array part in order.

`source <http://www.luafaq.org/#T1.10>`_
